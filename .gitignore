# Diagnostics land in HERDR_PLUGIN_STATE_DIR, not the plugin root, but a manual
# run with HERDR_PLUGIN_STATE_DIR unset falls back to the working directory.
*.log

# Test runs work out of mktemp directories, so nothing else here is generated.
.DS_Store
